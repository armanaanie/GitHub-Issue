
    - 1️⃣ What is the difference between var, let, and const?
    Answer:var is the old way to declare variables.It's not block scop.It can called from outside so it is a function scop.
    let and const are fuction scop.we can change let value easily by reassign it but const value is not reassignable.
    - 2️⃣ What is the spread operator (...)?
    Answer: spread operator(...) take every elements abd spread it and turn it in a array. 
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    Answer:These are array method but each one work different.
    map() is for transform array.It's return new transformed array.
    fiiter() is for filtering unwanted items;it has to pass a condition.It can also return new value based on condition.
    forEach() is for running code for all item,actually it's loop through an array and work on every item and it doesn't return anything.
    - 4️⃣ What is an arrow function?
    Answer: arrow function is just another way to write function in a shorter way.It gives cleaner look.Arrow function do not work on 'this'.Arrow function don't bind 'this' like other function do.
    - 5️⃣ What are template literals?
  Answer: Template literals are way to write strings dynamically using backtics(``) instead of quotation.

